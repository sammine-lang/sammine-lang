// RUN: %sammine --file %full %O && %T/%base.exe | %check %full

import io;

kernel sum_arr(a: [3]f64) -> f64 {
  reduce(a, +, 0.0)
}

let main() -> i32 {
  let a = [1.0, 2.0, 3.0];
  let s = sum_arr(a);
  io::printf("%f\n", s);
  0
}

// CHECK: 6.000000
