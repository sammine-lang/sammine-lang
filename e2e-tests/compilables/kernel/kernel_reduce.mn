// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK: 0

import io;

kernel sum_arr(a: [3]f64) -> f64 {
  reduce(a, +, 0.0)
}

let main() -> i32 {
  io::print_i32(0);
  io::printf("\n");
  0
}
