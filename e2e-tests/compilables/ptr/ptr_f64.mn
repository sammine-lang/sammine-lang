// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      2.72

import std;

let main() -> i32 {
  let x : f64 = 2.72;
  let p : ptr<f64> = &x;
  let y : f64 = *p;
  std::printf("%.2f\n", y);
  return 0;
}
