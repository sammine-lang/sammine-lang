// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      2.72

import std;

let main() -> i32 {
  let x : f64 = 2.72;
  let p = &x;
  let y = *p;
  std::printf("%.2f\n", y);
  return 0;
}
