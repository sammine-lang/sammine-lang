# RUN: ! %sammine --file %full --diagnostics 2>&1 | %check %full
# CHECK: Type mismatch in variable definition: Synthesized ptr<f64>, checked against ptr<i32>.
let main() -> i32 {
  let p : ptr<f64> = alloc(42);
  return 0;
}
