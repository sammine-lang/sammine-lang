# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK: 10
# CHECK-NEXT: 20

import std;

let main() -> i32 {
  let p : 'ptr<i32> = alloc<i32>(1);
  *p = 10;
  std::printf("%d\n", *p);
  *p = 20;
  std::printf("%d\n", *p);
  free(p);
  return 0;
}
