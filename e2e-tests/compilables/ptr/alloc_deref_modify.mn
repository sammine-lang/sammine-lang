# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 10
# CHECK-NEXT: 20

import std;

let main() -> i32 {
  let p : ptr<i32> = alloc(10);
  std::printf("%d\n", *p);
  *p = 20;
  std::printf("%d\n", *p);
  free(p);
  return 0;
}
