# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 10
# CHECK-NEXT: 20

let main() -> i32 {
  let p : ptr<i32> = alloc(10);
  printf("%d\n", *p);
  *p = 20;
  printf("%d\n", *p);
  free(p);
  return 0;
}
