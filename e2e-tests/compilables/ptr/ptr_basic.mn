# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      42

import std;

let main() -> i32 {
  let x : i32 = 42;
  let p : ptr<i32> = &x;
  let y : i32 = *p;
  std::printf("%d\n", y);
  return 0;
}
