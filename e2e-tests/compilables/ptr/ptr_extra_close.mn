# RUN: ! %sammine --file %full %O --diagnostics 2>&1 | %check %full
# CHECK:          |ptr_extra_close.mn:4:6
# CHECK:         3|let main() -> i32 {
# CHECK-NEXT:    4|  let p : ptr<i32>> = alloc<i32>(1);
# CHECK-NEXT:     |      ^^^^^^^
# CHECK-NEXT:     |      Missing '=' in variable definition — syntax is 'let name: type = value;'
# CHECK-NEXT:    5|  return 0;
# CHECK-NEXT:    6|}
let main() -> i32 {
  let p : ptr<i32>> = alloc<i32>(1);
  return 0;
}
