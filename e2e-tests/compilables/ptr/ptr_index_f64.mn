# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      3.14
# CHECK-NEXT: 2.72

import std;

let main() -> i32 {
  let mut p : 'ptr<f64> = alloc<f64>(2);
  p[0] = 3.14;
  p[1] = 2.72;
  std::printf("%.2f\n", p[0]);
  std::printf("%.2f\n", p[1]);
  free(p);
  return 0;
}
