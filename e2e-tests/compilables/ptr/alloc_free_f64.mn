# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 3.14

import std;

let main() -> i32 {
  let p : ptr<f64> = alloc<f64>(1);
  *p = 3.14;
  let y : f64 = *p;
  free(p);
  std::printf("%f\n", y);
  return 0;
}
