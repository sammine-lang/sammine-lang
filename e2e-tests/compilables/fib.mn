# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 34
let fib(x: i64) -> i64 {
  if x == 0 || x == 1 {
    return x;
  } else {
    return fib(x-1) + fib(x-2);
  };
}

let main() -> i64 {
  printf("%d\n", fib(9));
  return 0;
}
