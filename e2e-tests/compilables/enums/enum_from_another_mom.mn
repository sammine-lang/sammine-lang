// RUN: %sammine --file %dir/Inputs/dummy_enum.mn %O && %sammine_jit --diagnostics=dev --file %full %I -I %dir/Inputs | %check %full
// CHECK:      0
import dummy_enum;
import std;

let main() -> i32 {
  let x = dummy_enum::dummy::A;
  std::printf("%d", 0);
  return 0;
}
