// RUN: %sammine_jit --file %full | %check %full
// CHECK:      0

import std;

type Color = Red | Green | Blue;

let main() -> i32 {
  let c: Color = Color::Green;
  std::printf("%d\n", 0);
  return 0;
}
