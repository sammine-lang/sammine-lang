# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK: 25

import std;

let square(x: i32) -> i32 {
  return x * x;
}

let apply(f: (i32) -> i32, x: i32) -> i32 {
  return f(x);
}

let main() -> i32 {
  std::printf("%d\n", apply(square, 5));
  return 0;
}
