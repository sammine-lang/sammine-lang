# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 50

import std;

let square(x: i32) -> i32 {
  return x * x;
}

let double_it(x: i32) -> i32 {
  return x + x;
}

let main() -> i32 {
  std::printf("%d\n", 5 |> square |> double_it);
  return 0;
}
