# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      10

import std;

let add3(a: i32, b: i32, c: i32) -> i32 {
  return a + b + c;
}

let main() -> i32 {
  let add1_2: (i32) -> i32 = add3(1, 2);
  std::printf("%d\n", add1_2(7));
  return 0;
}
