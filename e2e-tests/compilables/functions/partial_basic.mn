# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      8

import std;

let add(a: i32, b: i32) -> i32 {
  return a + b;
}

let main() -> i32 {
  let add5: (i32) -> i32 = add(5);
  std::printf("%d\n", add5(3));
  return 0;
}
