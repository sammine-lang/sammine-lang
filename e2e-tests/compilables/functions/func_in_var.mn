# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      42

import std;

let inc(x: i32) -> i32 {
  return x + 1;
}

let main() -> i32 {
  let f: (i32) -> i32 = inc;
  std::printf("%d\n", f(41));
  return 0;
}
