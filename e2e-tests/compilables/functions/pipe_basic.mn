# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      25

import std;

let square(x: i32) -> i32 {
  return x * x;
}

let main() -> i32 {
  std::printf("%d\n", 5 |> square);
  return 0;
}
