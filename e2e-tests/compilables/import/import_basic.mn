# RUN: %sammine --file %dir/Inputs/math.mn && %sammine --file %full && ./import_basic.exe | %check %full
# CHECK: 7

import math as m;

import std;

let main() -> i32 {
  printf("%d\n", m::add(3, 4));
  return 0;
}
