// RUN: %sammine --file %dir/Inputs/math.mn %O && %sammine --file %full %O %I -I %dir/Inputs && %T/import_basic.exe | %check %full
// CHECK:      7

import math as m;

import std;

let main() -> i32 {
  std::printf("%d\n", m::add(3, 4));
  return 0;
}
