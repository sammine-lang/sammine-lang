// RUN: %sammine --file %dir/Inputs/has_generic.mn %O && %sammine_jit --file %full %I -I %dir/Inputs | %check %full
// CHECK:      42

import has_generic as g;
import std;

let main() -> i32 {
  let x: i32 = g::identity<i32>(42);
  std::printf("%d\n", x);
  return 0;
}
