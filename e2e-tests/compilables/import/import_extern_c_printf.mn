// RUN: %sammine --file %dir/Inputs/has_extern_c.mn %O && %sammine_jit --file %full %I -I %dir/Inputs | %check %full
// CHECK:      99

import has_extern_c as he;

let main() -> i32 {
  he::printf("%d\n", 99);
  return 0;
}
