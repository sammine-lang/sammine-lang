// RUN: %sammine --file %dir/Inputs/has_export_let.mn %O && %sammine_jit --file %full %I -I %dir/Inputs | %check %full
// CHECK:      hello from export let

import has_export_let as lib;

let main() -> i32 {
  lib::greet_c();
  return 0;
}
