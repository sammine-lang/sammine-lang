// RUN: %sammine --file %dir/Inputs/has_generic_array.mn %O && %sammine_jit --diagnostics=dev --file %full %I -I %dir/Inputs | %check %full
// CHECK:      10
import has_generic_array;
import std;

let main() -> i32 {
  let arr: [3]i32 = [10, 20, 30];
  let x = has_generic_array::first_elem<i32>(arr);
  std::printf("%d", x);
  return 0;
}
