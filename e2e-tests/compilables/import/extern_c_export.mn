# RUN: %sammine --file %dir/Inputs/has_extern_c.mn && cat has_extern_c.mni | %check %full

# reuse re-exports a C binding — both the reuse and let-functions
# should appear in .mni with mangled names
# CHECK: reuse has_extern_c$printf
# CHECK: reuse has_extern_c$greet

let main() -> i32 {
  return 0;
}
