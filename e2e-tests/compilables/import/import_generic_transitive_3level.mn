# RUN: %sammine --file %dir/Inputs/generic_level_c.mn %O && %sammine --file %dir/Inputs/generic_level_b.mn %O %I -I %dir/Inputs && %sammine --diagnostics=dev --file %full %O %I -I %dir/Inputs && %T/%base.exe | %check %full
# CHECK: 77
import generic_level_b;
import std;

let main() -> i32 {
  let result = generic_level_b::wrap_id<i32>(77);
  std::printf("%d", result);
  return 0;
}
