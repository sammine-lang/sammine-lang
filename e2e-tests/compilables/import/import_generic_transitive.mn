// RUN: %sammine --file %dir/Inputs/has_generic_deps.mn %O && %sammine --file %full %O %I -I %dir/Inputs && %T/import_generic_transitive.exe | %check %full
// CHECK:      99

import has_generic_deps as lib;
import std;

let main() -> i32 {
  let y: i32 = lib::apply<i32>(99);
  std::printf("%d\n", y);
  return 0;
}
