// RUN: %sammine --file %dir/Inputs/has_generic_diff_args2.mn %O && %sammine_jit --diagnostics=dev --file %full %I -I %dir/Inputs | %check %full
// CHECK:      99
import has_generic_diff_args2;
import std;

let main() -> i32 {
  let result = has_generic_diff_args2::first_as_i32<f64>(3.14, 99);
  std::printf("%d", result);
  return 0;
}
