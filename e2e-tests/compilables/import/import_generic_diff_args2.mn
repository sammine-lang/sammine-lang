# RUN: %sammine --file %dir/Inputs/has_generic_diff_args2.mn %O && %sammine --diagnostics=dev --file %full %O %I && %T/%base.exe | %check %full
# CHECK: 99
import has_generic_diff_args2;
import std;

let main() -> i32 {
  let result = has_generic_diff_args2::first_as_i32<f64>(3.14, 99);
  std::printf("%d", result);
  return 0;
}
