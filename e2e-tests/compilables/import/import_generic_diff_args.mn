// RUN: %sammine --file %dir/Inputs/has_generic_diff_args.mn %O && %sammine_jit --diagnostics=dev --file %full %I -I %dir/Inputs | %check %full
// CHECK:      42
import has_generic_diff_args;
import std;

let main() -> i32 {
  let result = has_generic_diff_args::apply_diff<f64>(3.14);
  std::printf("%d", result);
  return 0;
}
