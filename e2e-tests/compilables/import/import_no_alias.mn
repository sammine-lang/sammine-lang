// RUN: %sammine --file %dir/Inputs/math.mn %O && %sammine_jit --file %full %I -I %dir/Inputs | %check %full
// CHECK:      12

import math;

import std;

let main() -> i32 {
  std::printf("%d\n", math::multiply(3, 4));
  return 0;
}
