# RUN: ! %sammine --file %full 2>&1 | %check %full
# CHECK: Cannot find interface file 'nonexistent.mni'. Did you compile nonexistent.mn first?

import nonexistent as nx;

let main() -> i32 {
  return 0;
}
