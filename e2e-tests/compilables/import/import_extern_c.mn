# RUN: %sammine --file %dir/Inputs/has_extern_c.mn %O && %sammine --file %full %O %I -I %dir/Inputs && %T/import_extern_c.exe | %check %full
# CHECK:      hello from has_extern_c

import has_extern_c as he;

let main() -> i32 {
  he::greet();
  return 0;
}
