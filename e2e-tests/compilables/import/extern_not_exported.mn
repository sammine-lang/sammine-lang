# RUN: %sammine --file %dir/Inputs/has_extern.mn && cat has_extern.mni | %check %full

# reuse always re-exports — both reuse and let-functions appear in .mni
# CHECK: reuse has_extern$printf
# CHECK: reuse has_extern$greet

let main() -> i32 {
  return 0;
}
