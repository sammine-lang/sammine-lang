# RUN: %sammine --file %dir/Inputs/has_extern.mn && cat has_extern.mni | %check %full

# Externs are private to the module — only let-functions should appear in .mni
# The .mni should have the mangled name (has_extern$greet) but NOT printf
# CHECK: extern has_extern$greet
# CHECK-NOT: printf

let main() -> i32 {
  return 0;
}
