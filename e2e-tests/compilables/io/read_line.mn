// RUN: echo "hello stdin" | %sammine_jit --file %full | %check %full
// CHECK:      got: hello stdin

import io;

let main() -> i32 {
  let line_len: i64 = 0;
  let line: ptr<char> = io::read_line(&line_len);
  io::print("got: ");
  io::println(line);
  0
}
