# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 11

# Mutable parameter can be reassigned inside the function

import std;

let f(mut x: i32) -> i32 {
  x = x + 1;
  return x;
}

let main() -> i32 {
  let mut y: i32 = 10;
  std::printf("%d\n", f(y));
  return 0;
}
