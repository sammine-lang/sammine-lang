// RUN: %sammine_jit --file %full | %check %full
// CHECK:      5

// Passing mutable to immutable parameter is fine

import std;

let f(x: i32) -> i32 {
  return x;
}

let main() -> i32 {
  let mut y: i32 = 5;
  std::printf("%d\n", f(y));
  return 0;
}
