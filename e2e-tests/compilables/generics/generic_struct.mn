# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      42

import std;

struct Box<T> {
  value: T,
};

let main() -> i32 {
  let b = Box<i32>{ value: 42 };
  std::printf("%d", b.value);
  return 0;
}
