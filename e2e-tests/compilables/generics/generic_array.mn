# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK: 10

import std;

let first<T>(arr: [T; 3]) -> T { arr[0] }

let main() -> i32 {
  let a : [i32; 3] = [10, 20, 30];
  let x = first(a);
  std::printf("%d", x);
  return 0;
}
