// RUN: %sammine_jit --file %full | %check %full
// CHECK:      10

import std;

let first<T>(arr: [3]T) -> T { arr[0] }

let main() -> i32 {
  let a : [3]i32 = [10, 20, 30];
  let x = first(a);
  std::printf("%d", x);
  return 0;
}
