# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 10

import std;

let double(x: i32) -> i32 { x + x }

let apply(f: (T) -> U, x: T) -> U { f(x) }

let main() -> i32 {
  let result = apply(double, 5);
  std::printf("%d", result);
  return 0;
}
