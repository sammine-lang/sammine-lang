# RUN: ! %sammine --file %full %O --diagnostics 2>&1 | %check %full
# CHECK: Type 'T' not found in the current scope.

# Using T without declaring <T> should be an error.
let identity(x: T) -> T { x }

let main() -> i32 {
  let a = identity(42);
  return 0;
}
