# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 42

let deref_val(p: ptr<T>) -> T { *p }

let main() -> i32 {
  let x = 42;
  let p = &x;
  let result = deref_val(p);
  printf("%d", result);
  return 0;
}
