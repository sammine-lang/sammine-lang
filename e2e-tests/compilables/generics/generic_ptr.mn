// RUN: %sammine_jit --file %full | %check %full
// CHECK:      42

import std;

let deref_val<T>(p: ptr<T>) -> T { *p }

let main() -> i32 {
  let x = 42;
  let p = &x;
  let result = deref_val(p);
  std::printf("%d", result);
  return 0;
}
