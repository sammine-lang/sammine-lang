// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      10

import std;

let double(x: i32) -> i32 { x + x }

let apply<T, U>(f: (T) -> U, x: T) -> U { f(x) }

let main() -> i32 {
  let result = apply<i32, i32>(double, 5);
  std::printf("%d", result);
  return 0;
}
