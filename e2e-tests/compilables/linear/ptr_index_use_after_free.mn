// RUN: ! %sammine --file %full %O 2>&1 | %check %full
// CHECK: Cannot index 'p' — it was already freed previously

let main() -> i32 {
  let mut p : 'ptr<i32> = alloc<i32>(3);
  p[0] = 10;
  free(p);
  p[1] = 20;
  return 0;
}
