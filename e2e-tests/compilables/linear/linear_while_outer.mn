# RUN: ! %sammine --file %full %O --diagnostics 2>&1 | %check %full
# CHECK: Cannot consume{{.*}}inside a loop

let main() -> i32 {
  let p = alloc<i32>(1);
  let mut i : i32 = 0;
  while i < 3 {
    free(p);
    i = i + 1;
  };
  return 0;
}
