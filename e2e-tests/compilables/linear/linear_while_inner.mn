# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK: done

import std;

let main() -> i32 {
  let mut i : i32 = 0;
  while i < 3 {
    let p = alloc<i32>(1);
    *p = i;
    free(p);
    i = i + 1;
  };
  std::printf("done\n");
  return 0;
}
