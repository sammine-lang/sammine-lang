// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      99

import std;

let main() -> i32 {
  let p = alloc<i32>(1);
  *p = 99;
  let q = p;
  let y : i32 = *q;
  free(q);
  std::printf("%d\n", y);
  return 0;
}
