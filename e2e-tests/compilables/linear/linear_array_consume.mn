# RUN: ! %sammine --file %full %O --diagnostics 2>&1 | %check %full
# CHECK: already been consumed

let main() -> i32 {
  let p = alloc<i32>(1);
  let a : ['ptr<i32>;1] = [p];
  free(p);
  return 0;
}
