# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      42

import std;

let main() -> i32 {
  let p = alloc<i32>(1);
  *p = 42;
  let x : i32 = *p;
  let y : i32 = *p;
  free(p);
  std::printf("%d\n", x);
  return 0;
}
