// RUN: %sammine_jit --file %full | %check %full
// CHECK:      77

import std;

let main() -> i32 {
  let x : i32 = 77;
  let p : ptr<i32> = &x;
  let v : i32 = *p;
  std::printf("%d\n", v);
  return 0;
}
