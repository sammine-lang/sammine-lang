// RUN: %sammine_jit --file %full | %check %full
// CHECK:      done

import std;

let main() -> i32 {
  let p = alloc<i32>(1);
  if true {
    free(p);
  } else {
    free(p);
  };
  std::printf("done\n");
  return 0;
}
