# RUN: ! %sammine --file %full %O --diagnostics 2>&1 | %check %full
# CHECK: alloc is discarded

# CORRECT: Bare alloc<i32>(1) as a statement discards a linear pointer.
# The checker must reject this — it's a memory leak.

import std;

let main() -> i32 {
  alloc<i32>(1);
  std::printf("done\n");
  return 0;
}
