// RUN: %sammine_jit --file %full | %check %full
// CHECK:      7

import std;

let main() -> i32 {
  let p = alloc<i32>(1);
  *p = 7;
  let v : i32 = *p;
  free(p);
  std::printf("%d\n", v);
  return 0;
}
