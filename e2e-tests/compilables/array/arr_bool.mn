# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      1
# CHECK-NEXT: 0
# CHECK-NEXT: 1

import std;

let main() -> i32 {
  let a : [bool; 3] = [true, false, true];
  std::printf("%d\n", a[0]);
  std::printf("%d\n", a[1]);
  std::printf("%d\n", a[2]);
  return 0;
}
