# RUN: %sammine --file %full && ./%base.exe | %check %full
# CHECK: 10
# CHECK: 20
# CHECK: 30

import std;

let main() -> i32 {
  let a : [i32;3] = [10, 20, 30];
  std::printf("%d\n", a[0]);
  std::printf("%d\n", a[1]);
  std::printf("%d\n", a[2]);
  return 0;
}
