// RUN: %sammine --file %full %O && ! %T/%base.exe
// Verifies that writing to an out-of-bounds array index
// triggers the runtime bounds check and exits with non-zero.

import std;

let main() -> i32 {
  let mut a: [3]i32 = [1, 2, 3];
  let mut i: i32 = 5;
  a[i] = 99;
  return 0;
}
