# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      99

import std;

let main() -> i32 {
  let mut a : [3]i32 = [10, 20, 30];
  a[1] = 99;
  std::printf("%d\n", a[1]);
  return 0;
}
