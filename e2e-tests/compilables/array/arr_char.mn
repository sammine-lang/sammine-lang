# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      a
# CHECK-NEXT: b
# CHECK-NEXT: c

import std;

let main() -> i32 {
  let a : [char; 3] = ['a', 'b', 'c'];
  std::printf("%c\n", a[0]);
  std::printf("%c\n", a[1]);
  std::printf("%c\n", a[2]);
  return 0;
}
