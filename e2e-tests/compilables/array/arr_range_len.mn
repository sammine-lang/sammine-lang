// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      10
// CHECK-NEXT: 100

import std;

let main() -> i32 {
  let a : [10]i32 = [1...10];
  std::printf("%d\n", len(a));

  let b : [100]i32 = [1...100];
  std::printf("%d\n", len(b));
  return 0;
}
