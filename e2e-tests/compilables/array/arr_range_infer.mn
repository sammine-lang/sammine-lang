// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      1
// CHECK-NEXT: 5
// CHECK-NEXT: 5

import std;

let main() -> i32 {
  let a = [1...5];
  std::printf("%d\n", a[0]);
  std::printf("%d\n", a[4]);
  std::printf("%d\n", len(a));
  return 0;
}
