// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      not equal
// CHECK-NEXT: equal

import std;

let main() -> i32 {
  let a : i32 = 5;
  let b : i32 = 10;
  if a != b {
    std::printf("not equal\n");
  } else {
    std::printf("equal\n");
  };

  let c : i32 = 7;
  let d : i32 = 7;
  if c != d {
    std::printf("not equal\n");
  } else {
    std::printf("equal\n");
  };

  return 0;
}
