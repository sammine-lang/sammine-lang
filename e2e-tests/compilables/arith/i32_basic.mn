// RUN: %sammine_jit --file %full | %check %full
// CHECK:      42

import std;

let add(a : i32, b : i32) -> i32 {
  return a + b;
}

let main() -> i32 {
  let x : i32 = 40;
  let y : i32 = 2;
  std::printf("%d\n", add(x, y));
  return 0;
}
