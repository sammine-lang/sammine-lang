// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      -5
// CHECK-NEXT: 5

import std;

let main() -> i32 {
  let x : i32 = 5;
  std::printf("%d\n", -x);
  std::printf("%d\n", -(-x));
  return 0;
}
