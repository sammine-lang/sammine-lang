# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK: 42

import std;

let foo(i: i32) -> i32 {
  return i;
}

let main() -> i32 {
  let result : i32 = foo(42);
  std::printf("%d\n", result);
  return 0;
}
