// RUN: %sammine_jit --file %full --jit-args hello --jit-args world | %check %full
// CHECK:      argc = 3

import std;

let main(argc: i32, argv: ptr<ptr<char>>) -> i32 {
  std::printf("argc = %d\n", argc);
  return 0;
}
