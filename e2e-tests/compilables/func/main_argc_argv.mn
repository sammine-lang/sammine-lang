# RUN: %sammine --file %full %O && %T/%base.exe | %check %full
# CHECK:      argc = 1

import std;

let main(argc: i32, argv: ptr<ptr<char>>) -> i32 {
  std::printf("argc = %d\n", argc);
  return 0;
}
