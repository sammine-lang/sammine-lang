// RUN: %sammine --file %full %O && %T/%base.exe | %check %full
// CHECK:      2

import std;

let main() -> i32 {
  let x = if true {
    2;
  } else {
    3;
  };
  std::printf("%d", x);
  return 0;
}
