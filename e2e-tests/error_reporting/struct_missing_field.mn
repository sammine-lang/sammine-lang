# RUN: ! %sammine --file %full %O 2>&1 | %check %full
# CHECK: Struct 'Point' expects 2 fields, got 1

struct Point {
  x: i32,
  y: i32,
};

let main() -> i32 {
  let p: Point = Point { x: 1 };
  return 0;
}
