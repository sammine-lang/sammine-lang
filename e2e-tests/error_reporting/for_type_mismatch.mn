// RUN: ! %sammine --file %full %O --diagnostics 2>&1 | %check %full
// CHECK: for-range start and end must be the same type

import std;

let main() -> i32 {
  for i in 0i64..5u32 {
    std::printf("%ld\n", i);
  };
  return 0;
}
