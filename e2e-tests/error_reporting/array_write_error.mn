// RUN: ! %sammine --file %full %O 2>&1 | %check %full
// CHECK: Cannot write to array element. Arrays are immutable values

let main() -> i32 {
  let a: [3]i32 = [1, 2, 3];
  a[0] = 99;
  return 0;
}
