# RUN: ! %sammine --file %full 2>&1 | %check %full
# CHECK-ERR: No instance of Add<Foo>

struct Foo {
  x: i32,
};

let main() -> i32 {
  let a: Foo = Foo { x: 1 };
  let b: Foo = Foo { x: 2 };
  let c: Foo = a + b;
  return 0;
}
